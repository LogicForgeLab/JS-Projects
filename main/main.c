#include <stdio.h>
#include "dynamic_array.h"

int main ()
{
    Dynamic_Array arr;
    // init_array(&arr, 4);
    // print_array(&arr);

    // push_back(&arr, 10);
    // push_back(&arr, 20);
    // print_array(&arr);

    // push_front(&arr, 5);
    // insert(&arr, 15, 2);
    // print_array(&arr);

    // pop_back(&arr);
    // print_array(&arr);

    // pop_front(&arr);
    // print_array(&arr);

    // remove(&arr, 1);
    // print_array(&arr);

    // delete_array(&arr);
    return 0;
}